`server/app/storage.py:77` — SEVERITY(high) — `username` is unsanitized and used directly in filesystem paths (`data/<username>/...`), so values like `../...` can escape `DATA_DIR` and write logs outside the user namespace. fix: enforce a strict username regex at API boundary and `resolve()`/verify computed paths stay under `DATA_DIR` before any mkdir/open.  
`server/app/storage.py:162` — SEVERITY(critical) — `_seq` is recomputed by lexicographic month-file order, so appending an older-month event can renumber prior events and make new events invisible to clients using `since` cursor (data loss in pull semantics). fix: assign/persist immutable append-order sequence at write time (e.g., per-user counter) and query by that stable seq, not by re-deriving from month file ordering.  
`server/app/auth.py:75` — SEVERITY(med) — contract requires 30-day sliding sessions, but cookie refresh is only done on login/signup; authenticated reads don’t extend expiry. fix: re-issue `wt_session` on authenticated requests (middleware/dependency that validates then sets cookie).  
`client/src/lib/sync.js:137` — SEVERITY(med) — `flush()` deletes confirmed IDs from `outbox` before ensuring they are persisted into `events`; if `pull()` fails, those events are in neither local store, violating the outbox→events transition contract and risking local state gaps. fix: make transition atomic (keep until successful pull or use an intermediate “acked” state and finalize only after `events` write succeeds).  
`client/src/lib/db.js:34` — SEVERITY(med) — upgrade logic always deletes `events` whenever `onupgradeneeded` runs, so any future DB version bump will wipe synced history. fix: gate destructive migration by `oldVersion` and only recreate `events` when migrating from the legacy key schema.  
`client/src/lib/sync.js:194` — SEVERITY(low) — `startSync()` adds `online/offline` listeners but `stopSync()` never removes them, so relogin cycles accumulate handlers and duplicate flush triggers. fix: retain handler refs and `removeEventListener` in `stopSync()`.  
`setup.sh:83` — SEVERITY(low) — secrets are written before permissions are tightened (`chmod 600` happens after write), leaving a brief window where `.env` may be too-readable under permissive umask. fix: set restrictive umask (`umask 077`) before file creation or create with secure mode atomically.

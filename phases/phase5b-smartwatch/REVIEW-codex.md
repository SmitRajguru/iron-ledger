`client/src/Today.svelte:55` — HIGH — phone Today still contains its own session persistence/resolution block (`SESSION_KEY`, `loadPersisted`, `savePersisted`, `uniq`, hydrate/start logic) and does not use `lib/session.js`, so Phase 5b’s “shared today-session resolver” contract is not actually enforced across phone/watch. fix: refactor Today to import and use shared session helpers from `client/src/lib/session.js` for load/save/default/selection resolution paths.  
`client/src/lib/session.js:99` — HIGH — default-routine fallback runs even when a persisted session exists but is intentionally empty (e.g. `started:"custom"` with no exercises), so watch can show default-routine exercises while phone shows the user’s custom-empty day (state divergence). fix: only apply fallback when there is no persisted session (or persisted `started == null`), not merely when `selection` is empty.  
`client/src/Watch.svelte:13` — MEDIUM — watch uses `nextSetFromFold`/`lastSessionFromFold` directly instead of the shared live-store APIs `nextSetToLog`/`lastSessionFor` requested for Phase 5b, increasing coupling to fold internals. fix: switch watch prefill/last-session reads to `nextSetToLog(exerciseId, today)` and `lastSessionFor(exerciseId, today)`.  
`client/src/Watch.svelte:119` — HIGH — `confirming` is cleared immediately after `queue(ev)` returns, not after `prefill.set_index` advances, so a quick second tap can enqueue another event at the same stale set index (duplicate/overwrite race). fix: mirror the phone guard (`confirmingFromIndex`) and release only when fold recompute advances past the confirmed index.  
`client/src/Watch.svelte:23` — MEDIUM — watch’s `today` re-pins only on `$logState` updates, so an idle watch page across local midnight can keep logging to the previous date until another log/sync mutation happens. fix: add the same periodic/focus/visibility date refresh pattern used in `Today.svelte`.

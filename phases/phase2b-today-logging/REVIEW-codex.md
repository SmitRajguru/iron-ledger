`client/src/lib/logging.js:185` — SEVERITY(critical) — `nextSetFromFold` uses `today.length` as next `set_index`; after a tombstone creates index holes, the next log can reuse an existing index and overwrite a different set projection. fix.  
`client/src/lib/logging.js:202` — SEVERITY(med) — Prefill priority is misimplemented: when last-session has no matching `set_index`, code falls back to the last set instead of the contract’s empty/default branch. fix.  
`server/app/events.py:168` — SEVERITY(critical) — `_validate_set_logged_payload` always requires `unit` and `warmup`, so client delete tombstones (`deleted:true` with identity + `voids`) are rejected and deleted sets can reappear after outbox drains. fix.  
`client/src/ExerciseCard.svelte:145` — SEVERITY(critical) — Warmup-toggle edit reconstructs stored weights through `toDisplay(...)->toBase(...)`, which rounds to display half-steps and silently mutates previously logged load values (weight/added_weight corruption). fix.  
`client/src/ExerciseCard.svelte:260` — SEVERITY(critical) — Confirm is not click-locked; rapid double-tap can enqueue multiple events with the same stale `prefill.set_index`, collapsing distinct taps into one projected set (set-loss/dup bug). fix.  
`client/src/Today.svelte:25` — SEVERITY(high) — `sessionDate` is captured once at component init; logging across local midnight keeps writing the prior LA date, misfiling sets into the wrong session. fix.  
`client/src/Today.svelte:92` — SEVERITY(med) — Bodyweight save queues measurement asynchronously but assisted logging can happen before projection rebuild, so `bodyweight_snapshot` may remain stale/null even right after “Save bodyweight”. fix.  
`client/src/Today.svelte:177` — SEVERITY(low) — Contract-required auto-scroll active exercise into view is missing (no active-card tracking and no `scrollIntoView` path wired from `setlogged`). fix.

- **F1 — FIXED:** `client/src/AppShell.svelte` no longer has the dev “Emit test event” control, the rejected note is dismissible (`dismissRejectedNote`), and `client/src/lib/sync.js` `seedRejectedIds()` purges deadletters only when `event.type` is not in `REAL_EVENT_TYPES` (so real `set_logged` rejects are retained).
- **F2 — FIXED:** In `client/src`, user-facing copy is now “saved on this device” (seen in `AppShell.svelte`, `ExerciseCard.svelte`, `Login.svelte`), with no remaining “saved on this phone” strings.
- **F3 — FIXED:** `client/src/Today.svelte` persists per-date session state under `wt_today_session_<date>`, hydrates on `effectiveDate` change/remount, and renders `selection ∪ loggedIds` (no selected+logged duplication; logged history remains visible even if no longer selected).
- **F4 — NOT-FIXED:** `client/src/ExerciseCard.svelte` does reactive in-field unit conversion, but it rounds converted drafts to 0.5 (`Math.round(convert(...)*2)/2`), so toggling units can introduce precision loss before storage (violates “no rounding into storage” from converted in-field values).
- **F5 — NOT-FIXED:** `client/src/lib/logging.js` correctly enforces strictly-before prefill (`d < beforeDate`) and `effectiveDate` is the card’s single source for prefill + `session_date`, but `client/src/Today.svelte` updates `todayStr` only when `$logState` changes, so “today” is not truly live across idle midnight rollover.

- **NEW bug [HIGH]:** In `client/src/Today.svelte`, `swapExercise()` can create duplicate exercise IDs if swapping to an already-selected exercise (no dedupe), which can yield duplicate keyed rows (`{#each sessionExerciseIds as exId (exId)}`) and unstable/crashing render behavior.

- **H1 measurement delete:** **FIXED** — `Body.svelte` delete now emits measurement tombstone via `buildMeasurementDeleteEvent` (`{deleted:true}` + `voids`), `foldLog` in `client/src/lib/logging.js` drops voided/tombstoned measurements and recomputes `latestBodyweight`, `server/app/events.py` accepts tombstone shape, and sync drain/reload still folds server `events` the same way so deleted measurements stay gone.
- **H2 progression add_reps:** **FIXED** — `progressionSuggestion` in `client/src/lib/analytics.js` now does hit-top+no-inc => `add_reps`, assisted+inc => `reduce_assist`, non-assisted+inc => `add_weight`, miss => `null`; `ExerciseCard.svelte` apply path for `add_reps` only increments reps (+1) and keeps load untouched.
- **M1 stale note realism:** **FIXED** — `buildSetPayload` stamps `bodyweight_snapshot_date` only when snapshot+date exist, `exerciseSeries` carries `bwSnapshotDate`, `Graphs.svelte` stale note only triggers when `(session_date - bwSnapshotDate) > 30` and is absent when date is missing, and server validation accepts optional `bodyweight_snapshot_date`.
- **M2 first_time gating:** **FIXED** — `progressionSuggestion` returns `null` when rep range is missing, `first_time`/`add_reps` do not require increment, and `add_weight`/`reduce_assist` are gated on `inc != null`.
- **L1 increment interpolation:** **FIXED** — analytics now returns `message: null` for `add_weight`/`reduce_assist`, and `ExerciseCard.svelte` formats UI text with real display-unit increment (`toDisplay(abs(delta))`).
- **L2 one-tap mute:** **FIXED** — hint row in `ExerciseCard.svelte` has a `mute` action that emits `exercise_updated` with `hold_progression: true` (via `buildExercisePayload` + `queue`).

**NEW bugs found**
- **[MED] Deleted pending bodyweight can still be used for snapshots** — `buildMeasurementDeleteEvent` does not clear `pendingBodyweight`, and `freshestBodyweightFull` only drops pending on id match/newer projected date/rejection; if a pending BW entry is tombstoned, assisted set logging can still stamp stale/wrong `bodyweight_snapshot`/`bodyweight_snapshot_date`.
- **[LOW] Progression hints can use future sessions when backfilling past dates** — `progressionSuggestion` has no date cutoff and `ExerciseCard` calls it without `effectiveDate`, so logging an older day can get hints based on sessions that happened later.

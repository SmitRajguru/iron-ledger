- 1) **FIXED** — `foldLog()` now exposes `voided`, `freshestBodyweightFull()` drops `pendingBodyweight` when its id is voided, `buildMeasurementDeleteEvent()` clears pending immediately for tombstoned pending ids, and `buildMeasurementEventFull()` clears pending on void-without-new-bodyweight, so the deleted/tombstoned pending BW path no longer reaches `bodyweight_snapshot` in set events.
- 2) **FIXED** — `progressionSuggestion(folded, exercise, beforeDate)` uses strict `d < beforeDate`, `ExerciseCard.svelte` passes `effectiveDate`, so backfilled past-day hints cannot use later sessions; omitting `beforeDate` still includes all sessions (unchanged default behavior).
- **NEW bug found:** none in the inspected voided-set wiring / `beforeDate` cutoff paths (no tombstone-target omission in `voided`, no `<=` same-day leak, no extra pending-clear branch beyond `voids === pending.id` with no new BW).

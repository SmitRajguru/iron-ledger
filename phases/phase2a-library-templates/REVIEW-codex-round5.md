FIXED — (1) no remaining edit-path reads of `ordered_exercise_ids`/addable from stale `$routinesMap` (they flow from `effectiveRoutine`/`orderedIds`), (2) add-then-immediate-reorder is preserved by `saveOrder` appending freshest missing ids, (3) double-add is blocked by `addExercise` no-op on existing id, (4) reconcile keeps dragged-current ids then appends missing-current ids (no accidental drop/dup in normal unique-id DnD flow), (5) `effectiveRoutine.__src_id` source (`pending ?? routinesMap`) matches `emitRoutine` void target source (`freshest`), and no new reconcile/dedup/effectiveRoutine bug is apparent.

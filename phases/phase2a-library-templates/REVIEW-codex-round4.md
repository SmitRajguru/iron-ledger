- **1) HIGH lost-update race — NOT-FIXED:** `emitRoutine` now uses pending-authoritative reads/writes and correct void chaining, but a stale path remains in `saveOrder(ids)` (`client/src/Routines.svelte`) because `ids` comes from UI built on `$routinesMap` (not pending), so add/remove then immediate reorder in the pre-rebuild window can drop newer pending list changes.
- **2) brick-on-failure — FIXED:** `mutateChain` is failure-isolated via `.catch(...)` in `client/src/Routines.svelte`, so one `queue()` rejection no longer bricks subsequent edits, and failures after outbox write are retried by normal sync.
- **3) MED _enq seeding — FIXED:** `seedEnq()` is a memoized promise (`enqSeedPromise`) and `queue()` awaits it before `nextEnq()` in `client/src/lib/sync.js`, so `_enq` assignment cannot race ahead of seed completion; old sync-flag early-return race is gone.
- **NEW bug:** rapid double-add of the same exercise can create duplicate `exercise_id` entries (stale `addable` from `$routinesMap` + pending-based append in `addExercise`), which violates the one-item-per-exercise assumption and can break keyed list behavior.
